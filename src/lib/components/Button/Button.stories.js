import Button from "./Button";

export default {
  title: "Atoms/Button",
  component: Button,
  label: "Button",
};

const Template = (args) => <Button {...args} />;

export const Default = Template.bind({});
